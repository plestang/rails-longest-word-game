<h1>New Game</h1>
<p>letters :</p>
 <% @letters.each do |letter| %>
  <p class="btn btn-primary shadow"> <%= letter  %>
  </p>
  <% end %>


<form action="/score" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
  <input type="text" name="word" placeholder="Word to be submitted">
  <input type="submit" value="Play" class="btn btn-secondary shadow">
</form>
